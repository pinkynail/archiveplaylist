<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Archive Playlist</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Archive Playlist</h1>
    <form method="POST" action="/download" class="mb-4">
      <div class="mb-3">
        <input type="text" name="youtube_url" class="form-control" placeholder="Вставь ссылку на YouTube" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Выбери плейлист:</label>
        <select name="folder_id" class="form-select">
          <option value="">Создать новый плейлист</option>
          <% folders.forEach(folder => { %>
            <option value="<%= folder.id %>"><%= folder.name %></option>
          <% }) %>
        </select>
      </div>
      <div class="mb-3">
        <input type="text" name="new_folder_name" class="form-control" placeholder="Имя нового плейлиста (если выбран 'Создать')">
      </div>
      <button type="submit" class="btn btn-primary w-100">Сохранить</button>
    </form>
    <h2>Существующие плейлисты:</h2>
    <ul class="list-group">
      <% if (folders.length === 0) { %>
        <li class="list-group-item">Плейлистов пока нет</li>
      <% } else { %>
        <% folders.forEach(folder => { %>
          <li class="list-group-item"><%= folder.name %> (<%= folder.songs.length %> песен)</li>
        <% }) %>
      <% } %>
    </ul>
  </div>
</body>
</html>
