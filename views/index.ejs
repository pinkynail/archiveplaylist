<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Archive Playlist</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container mt-5">
    <div class="card p-4 shadow-sm">
      <h1 class="text-center mb-4">Archive Playlist</h1>
      <form method="POST" action="/download">
        <div class="mb-3">
          <input type="text" name="youtube_url" class="form-control" placeholder="Вставь ссылку на YouTube" required>
        </div>
        <div class="mb-3">
          <input type="text" name="new_folder_name" class="form-control" placeholder="Имя нового плейлиста (опционально)">
        </div>
        <div class="mb-3">
          <select name="folder_id" class="form-select">
            <option value="">Выбери плейлист:</option>
            <% folders.forEach(folder => { %>
              <option value="<%= folder.id %>"><%= folder.name %></option>
            <% }); %>
          </select>
        </div>
        <button type="submit" class="btn btn-primary w-100">Сохранить</button>
      </form>
      <form method="POST" action="/clear" class="mt-3">
        <button type="submit" class="btn btn-danger w-100">Очистить всё</button>
      </form>
      <h2 class="mt-4">Существующие плейлисты:</h2>
      <ul class="list-group">
        <% folders.forEach(folder => { %>
          <li class="list-group-item"><%= folder.name %></li>
        <% }); %>
      </ul>
    </div>
  </div>
</body>
</html>
